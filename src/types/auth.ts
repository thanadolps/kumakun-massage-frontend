type UserData = {
  _id: string;
  name: string;
  email: string;
  role: string;
  tel: string;
  isVerified: boolean;
};

type RegisterUserData = {
  name: string;
  password: string;
  email: string;
  role: string;
};
